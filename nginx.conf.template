events {}

stream {
    upstream backend {
        server ${NGINX_UPSTREAM};
    }

    server {
        listen ${NGINX_LISTEN_PORT};
        proxy_pass backend;
        proxy_connect_timeout 10s;
    }
}
